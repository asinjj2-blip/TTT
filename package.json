{
  "name": "ttt-tiktok-lookup",
  "version": "1.0.0",
  "private": true,
  "description": "Public TikTok profile lookup with explicit account-region signals only.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
